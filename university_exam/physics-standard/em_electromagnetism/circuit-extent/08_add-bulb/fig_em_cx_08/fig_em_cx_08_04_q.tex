\documentclass[border=1pt]{standalone}

\usepackage{tikz}
\usepackage{luatexja}

\usepackage{circuitikz}

\ctikzset{resistors/width=1.1, batteries/scale=1.2, 
    capacitors/width=0.2, capacitors/height=0.9,
    grounds/scale=1.6}
\ctikzset{bipoles/cuteswitch/thickness=0.23}
\ctikzset{misc/scale=0.8}

\begin{document}

\begin{circuitikz}
       
    \pgfmathsetmacro{\xa}{2.3}
    \pgfmathsetmacro{\xb}{3.6}
    \pgfmathsetmacro{\xc}{3*\xb/9}    
    \pgfmathsetmacro{\ya}{4}
    \pgfmathsetmacro{\yb}{\ya/3}
    
    \pgfmathsetmacro{\d}{1.5}

    \draw (0,0) to [battery1, invert, l_=$E$] ++ (0,\yb)
        to [european resistor, l_=$r$] ++ (0,\ya-\yb) -- (\xa, \ya)
        to [lamp] ++ (0,-\ya/2) 
        to [lamp] ++ (0,-\ya/2) 
        to [european resistor, l=$R_3$] ++ (-\xa,0);
    \node [yshift=10pt] at (\xa/2,\ya) {A};

    \draw (\xa+\d,0) to [battery1, invert, l_=$E$] ++ (0,\yb)
        to [european resistor, l_=$r$] ++ (0,\ya-\yb)
        to [short,-*] ++ (\xb-\xc,0)
        to [short] ++ (\xc,0)
        to [lamp] ++ (0,-\ya) 
        to [short,-*] ++ (-\xc,0) 
        to [european resistor, l=$R_3$] ++ (-\xb+\xc,0);
    \draw (\xa+\d+\xb-\xc,0) to [lamp] ++ (0,\ya);
    \node [yshift=10pt] at (\xa+\d+\xb/2,\ya) {B};
\end{circuitikz}

\end{document}
